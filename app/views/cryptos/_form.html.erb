<%= form_with(model: crypto, local: true) do |form| %>
  <% if crypto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crypto.errors.count, "error") %> prohibited this crypto from being saved:</h2>

      <ul>
      <% crypto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol, id: :crypto_symbol %>    
  </div>

  <div class="field">
    
    <%= form.number_field :user_id, value: current_user.id, type: "hidden"  %>
  </div>

  <div class="field">
    <%= form.label :amount_owned %>
    <%= form.text_field :amount_owned, id: :crypto_amount_owned %>
  </div>

  <div class="field">
    <%= form.label "Wallet location" %>
    <%= form.text_field :location, id: :crypto_location %>
  </div>

   <div class="field">
    <%= form.label "Date of purchase" %>
    <%= form.date_field :date, id: :crypto_date %>
  </div>
  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
